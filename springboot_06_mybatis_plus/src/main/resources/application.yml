#2.配置相关信息
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver #新驱动
    url: jdbc:mysql://localhost:3306/ssm_db
    username: root
    password: 1234
#设置mybatis_plus相关的配置
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tbl_

